# AUTOMATSUO V3 — Dependências
# pip install -r requirements.txt

# Obrigatórias
pandas>=2.0.0
openpyxl>=3.1.0

# Automação real (Windows only)
# pyautogui>=0.9.54
# pygetwindow>=0.0.9

# Tkinter — já incluso no Python padrão.
# Linux (Ubuntu/Debian): sudo apt install python3-tk
